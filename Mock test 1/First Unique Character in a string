char getFirstRepeatingChar(string &s) {
   unordered_set<char> hash;
   for (int i=0; i<s.length(); i++) {
      char c = s[i];      
   }
   for (int i = 0; i < s.length(); i++)
   {
       if (hash.count(s[i]) == 1) return s[i];
   }
   return '\0';
}
